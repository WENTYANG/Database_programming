#include "exerciser.h"

void exercise(connection* C) {
    // query1(C, 1, 35, 40, 0, 0, 0, 0, 5, 10, 0, 0, 0, 0, 0, 0, 0, 0, 0);
    // query1(C, 0, 35, 40, 0, 0, 0, 1, 5, 10, 0, 0, 0, 0, 0, 0, 0, 0, 0);
    // query2(C, "Green");
    // query2(C, "Orange");
    // query3(C, "Duke");
    // query3(C, "UNC");
    query4(C, "MA", "Maroon");
    query4(C, "NC", "DarkBlue");
    // query5(C, 13);
    // query5(C, 10);
    // add_player(C, 1, 2, "Wenting", "Yang", 3, 4, 5, 6, 7.0, 8.0);
    // add_player(C, 1, 2, "Yang", "Wenting", 3, 4, 5, 6, 7.0, 8.0);
    // add_team(C, "test_team1", 3, 1, 10, 9);
    // add_team(C, "test_team2", 3, 1, 10, 9);
    // add_state(C, "test_NC");
    // add_state(C, "test_MA");
    // add_color(C, "test_yellow");
    // add_color(C, "test_white");
}
